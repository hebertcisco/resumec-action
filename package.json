{
  "name": "resumec-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action for validating and building resumes with resumec",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js --minify --source-map --license licenses.txt",
    "test": "node --test",
    "check": "npm run test && npm run build"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@actions/core": "3.0.1",
    "@actions/exec": "3.0.0",
    "@actions/tool-cache": "4.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "0.38.4"
  },
  "license": "MIT"
}

