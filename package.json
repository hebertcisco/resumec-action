{
  "name": "resumec-action",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GitHub Action for validating and building resumes with resumec",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test",
    "check": "node --test && node --check src/index.js && node --check src/main.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {},
  "devDependencies": {},
  "license": "MIT"
}
